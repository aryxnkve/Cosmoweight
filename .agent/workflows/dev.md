---
description: Start the Cosmic Weight Calculator dev server and open it in the browser
---

// turbo-all

1. Start the Vite dev server:
```
npm run dev
```
(Working directory: `c:\Planetary_weight`)

2. Open the browser to `http://localhost:5173` and take a screenshot to verify it loaded.
